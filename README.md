# builds

Esta rama es generada automáticamente por [`.github/workflows/build.yml`](https://github.com/fbientrigo/stream/blob/main/.github/workflows/build.yml).

Contiene los `.cs3` compilados y `plugins.json`, que es lo que consume la app de Cloudstream a través de `repo.json`. No edites esta rama a mano: cualquier cambio se sobrescribirá (`git push --force`) en el próximo build.
